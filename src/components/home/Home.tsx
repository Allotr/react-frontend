import React, { useEffect } from "react";

function Home(props: { value: string }) {
  useEffect(() => {
    // Add your init code
  }, [])

  return (
    <h1>This is Home</h1>
  );
}

export default Home;